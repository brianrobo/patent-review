제공해주신 Summary of the Invention은 가짜 기지국을 판별하는 두 가지 구체적인 실시예(Embodiment)를 다루고 있습니다.
첫 번째는 **매개변수 불일치(Inconsistency)**를 기준으로 하는 일반적인 방법이고, 두 번째는 **신호 강도(Power)와 지속 시간(Lifetime)**이라는 구체적인 물리 지표를 결합한 방법입니다. 이를 구분하여 다이어그램으로 표현해 드립니다.
📊 US9867039B2 Summary: Detection Methods
graph TD
    Start([Detection Triggered]) --> Device["1st Wireless Device (UE or BS)"]
    Device --> Detect["Detect Discovery Signal from<br/>2nd Wireless Device (Candidate)"]

    %% Split into two embodiments
    Detect --> Emb1["<b>Embodiment 1: General Comparison</b>"]
    Detect --> Emb2["<b>Embodiment 2: Physical Metrics Logic</b>"]

    subgraph "Method A: Parameter Consistency"
        Emb1 --> Params[Identify Multiple Parameters]
        Params --> Compare1[Compare with Neighbor Cluster Parameters]
        Compare1 --> Incon{Inconsistency ><br/>Threshold?}
        Incon -- "Yes" --> Fake1[Determined as Fake BS]
    end

    subgraph "Method B: Power-Duration Logic"
        Emb2 --> Ref[Determine Cluster's Ref:<br/>Power Threshold & Min Lifetime]
        Ref --> Cond{Check Conditions}
        Cond -- "1. Power > Threshold<br/>AND<br/>2. Lifetime < Min Duration" --> Fake2[Determined as Fake BS]
    end

    Fake1 --> Action([Responsive Action])
    Fake2 --> Action

💡 핵심 내용 분석
1. Embodiment 1 (일반적 매개변수 비교)
 * 가짜 기지국이 보내는 신호에서 여러 파라미터를 추출합니다.
 * 이 데이터들을 이미 알고 있는 **주변 기지국 클러스터(Cluster of neighboring BSs)**의 설정값들과 대조합니다.
 * 그 차이(Inconsistency)가 허용 범위(Threshold)를 넘어서면 가짜로 간주합니다.
2. Embodiment 2 (전력 및 생존 시간 논리) 🌟 중요 포인트
 * 이 방식은 가짜 기지국의 전형적인 행동 패턴을 공략합니다.
   * 강한 전력(High Power): 단말의 연결을 뺏어오기 위해 신호를 강하게 쏩니다.
   * 짧은 생존 시간(Short Lifetime): 단속을 피하거나 이동하며 작동하기 때문에 특정 위치에서 감지되는 시간이 짧습니다.
 * 판단 공식: (수신 전력 > 임계치) 이고 동시에 (신호 유지 시간 < 최소 유지 시간) 일 경우 가짜 기지국으로 확정합니다.
요약(Summary) 파트까지 파악이 완료되었습니다. 이제 다음 순서인 **[Background of the Invention(발명의 배경)]**으로 이동하여 왜 이런 방식이 고안되었는지 확인해 볼까요? 아니면 바로 **[Claims(청구항)]**의 상세 조건을 보시겠습니까?



graph TD
    %% Start
    Start([탐지 프로세스 시작]) --> Device["1st Device<br/>(UE 또는 BS)"]
    
    Device --> Detect["2nd Device의<br/>검출 신호 감지"]

    %% 두 가지 실시예 분기
    Detect --> Emb1["<b>[방법 1]</b><br/>매개변수 일관성 비교"]
    Detect --> Emb2["<b>[방법 2]</b><br/>전력 및 지속시간 분석"]

    %% Embodiment 1 상세
    subgraph "일관성 체크 (Inconsistency)"
        Emb1 --> Params["여러 매개변수<br/>(Parameters) 식별"]
        Params --> Compare1["주변 기지국<br/>클러스터와 비교"]
        Compare1 --> Incon{"불일치 정도가<br/>임계치를<br/>초과하는가?"}
        Incon -- "Yes" --> Fake1["가짜 기지국<br/>(Fake BS) 판정"]
    end

    %% Embodiment 2 상세
    subgraph "물리 지표 분석 (Power & Lifetime)"
        Emb2 --> Ref["기준점 결정:<br/>전력 임계치 &<br/>최소 지속시간"]
        Ref --> Cond{"[조건 확인]<br/>1. 전력 > 임계치<br/>AND<br/>2. 지속시간 < 최소값"}
        Cond -- "조건 충족" --> Fake2["가짜 기지국<br/>(Fake BS) 판정"]
    end

    %% Result
    Fake1 --> Action([대응 조치 수행])
    Fake2 --> Action

